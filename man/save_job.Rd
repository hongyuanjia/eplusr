% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/job-json.R
\name{save_job}
\alias{save_job}
\alias{read_job}
\title{Save and Read EnergyPlus Job Manifests}
\usage{
save_job(x, path = NULL, overwrite = FALSE, relative = TRUE, hash = TRUE)

read_job(path, validate = TRUE, verify = c("warn", "error", "ignore"))
}
\arguments{
\item{x}{An \link{EplusJob}, \link{EplusGroupJob}, or \link{ParametricJob}.}

\item{path}{A JSON file path. If \code{NULL}, a default file name is used in the
input model directory.}

\item{overwrite}{Whether an existing manifest, and any generated
\link{ParametricJob} model snapshots, may be overwritten.}

\item{relative}{Whether paths under the manifest directory should be written
as relative paths.}

\item{hash}{Whether to hash existing EnergyPlus result files. File size and
modification time are still recorded when \code{FALSE}.}

\item{validate}{Whether to validate the JSON manifest against eplusr's job
manifest schema before restoring the object.}

\item{verify}{How to handle saved EnergyPlus result files whose current
checksum or metadata no longer match the manifest. \code{"warn"} restores the
job and emits a warning, \code{"error"} stops, and \code{"ignore"} skips the check.}
}
\value{
\code{save_job()} invisibly returns the manifest path. \code{read_job()} returns
an \link{EplusJob}, \link{EplusGroupJob}, or \link{ParametricJob}.
}
\description{
\code{save_job()} writes a JSON manifest for an \link{EplusJob}, \link{EplusGroupJob}, or
\link{ParametricJob}. \code{read_job()} reads that manifest, validates its structure,
verifies saved output file metadata, and restores the job object so existing
simulation outputs can be collected.
}
\details{
The JSON manifest stores references to input and output files instead of
embedding EnergyPlus files. By default, paths under the manifest directory
are stored relative to the manifest so the directory can be moved together.
EnergyPlus result files are also recorded with file size, modification time,
and a checksum when available. These file metadata are informational and do
not prevent \code{read_job()} from restoring the job object if files have been
removed.
}
\section{Model snapshots}{

The manifest stores a prepared model snapshot store for \link{EplusJob},
\link{EplusGroupJob}, and \link{ParametricJob}. For \link{ParametricJob}, arbitrary R
measure functions passed to \verb{$apply_measure()} are not serialized; the
restored job uses the generated model snapshots and stored case data.
}

\examples{
\dontrun{
job <- eplus_job(path_idf, path_epw)
job$run(echo = FALSE)

path <- save_job(job, tempfile(fileext = ".json"))
restored <- read_job(path)
restored$status()
}

}
